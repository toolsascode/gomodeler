version: "2"
run:
  go: "1.23"
linters:
  enable:
    - bodyclose
    - copyloopvar
    - depguard
    - forbidigo
    - gocritic
    - misspell
    - nolintlint
    - revive
    - tagliatelle
    - testifylint
    - thelper
    - tparallel
    - unconvert
    - unparam
    - wastedassign
  settings:
    depguard:
      rules:
        main:
          deny:
            - pkg: github.com/pkg/errors
              desc: use stdlib instead
    forbidigo:
      forbid:
        - pattern: ioutil\.*
    gocritic:
      disabled-checks:
        - appendAssign
    staticcheck:
      checks:
        - -SA1019
        - all
    tagliatelle:
      case:
        rules:
          json: snake
          yaml: snake
        use-field-name: false
    testifylint:
      enable-all: true
      disable:
        - error-is-as
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
